Apache Hive basics - Process structure data in Hadoop.

#Hive concepts
--------------
*)Storing data in Hive 
      - Data(stored in HDFS)
      - Metadata (stored in Metastore(RDBMS)
      
*)Complex data types
   1.)Array
   2.)Map
   3.)Struct
   
*)Built-in functions (3 Categories)
  1) UDF (User defined functions)
  2)UDAF (User defined aggregate function)
  3)UCTF (User defined table generating functions)
  

